{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user of the LumenAI application.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the application.",
          "enum": ["homeowner", "installer"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user account was created."
        },
        "serviceLocations": {
          "type": "string",
          "description": "The areas or cities where the installer provides services. Only for installers."
        },
        "companyName": {
          "type": "string",
          "description": "The installer's company name. Only for installers."
        },
        "companyDescription": {
          "type": "string",
          "description": "A short description of the installer's company and services. Only for installers."
        },
        "pricingDetails": {
          "type": "string",
          "description": "A description of the installer's pricing model (e.g., '$$ - Competitive'). Only for installers."
        }
      },
      "required": ["displayName", "email", "role", "createdAt"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/User"
      },
      "description": "Stores public information about a user."
    }
  }
}
